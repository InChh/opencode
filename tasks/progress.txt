## Codebase Patterns
- Nested config defaults can live directly in the Zod schema in `packages/opencode/src/config/config.ts`; cover them with `Config.get()` integration tests in `packages/opencode/test/config/config.test.ts` so parsing and merge behavior are validated together.
- Keep Hindsight bank identity and resolve rules in dedicated helpers under `packages/opencode/src/memory/hindsight/`; only `mem:` ids should resolve directly to authoritative memories, while `sess:` and `obs:` remain indirect ranking evidence and cross-worktree hashes must be dropped.
- Workspace-scoped companion services fit `Instance.state(..., dispose)` well: cache one lazy boot promise per worktree, bind the embedded daemon to `127.0.0.1`, and treat startup or health timeouts as degraded fallback instead of throwing into memory flows.
- When Hindsight needs shared timeout or fallback behavior across retain, recall, and inspect paths, wrap the SDK once in `packages/opencode/src/memory/hindsight/client.ts` and keep downstream memory flows on non-fatal `undefined` results instead of scattering per-call error handling.
- Authoritative-memory retain helpers should build `document_id`, metadata, and tags through `MemoryHindsightMap`, force `update_mode: "replace"`, and return structured non-fatal results so local writes never depend on companion success.
- Hindsight recall wrappers should tolerate SDK hit-shape drift (`results` vs `items`, `score` vs relevance-score aliases), preserve source rank on resolved candidates, and record stale or indirect drops separately so downstream recall only sees authoritative local memories.

## 2026-04-14 23:20:31 CST - US-001
- Added `memory.hindsight` config parsing in `packages/opencode/src/config/config.ts` with the requested feature gates, rollout knobs, and phase-1 defaults.
- Files changed: `packages/opencode/src/config/config.ts`, `packages/opencode/test/config/config.test.ts`, `tasks/prd.json`, `tasks/progress.txt`
- **Learnings for future iterations:**
  - Config defaults that must survive merge and file parsing are safest when defined on the schema itself instead of in downstream runtime helpers.
  - `packages/opencode/test/config/config.test.ts` is the right place for end-to-end config parsing coverage because it exercises real config-file loading through `Config.get()`.
  - Restricting phase-1 enums like Hindsight `mode` and `workspace_scope` at schema time gives immediate parse failures and keeps rollout guards centralized.
---

## 2026-04-15 10:32:00 CST - US-004
- Added `packages/opencode/src/memory/hindsight/bank.ts` and `packages/opencode/src/memory/hindsight/mapper.ts` for deterministic worktree hashing, `opencode:${worktreeHash}` bank ids, stable `mem:`, `sess:`, and `obs:` document ids, normalized memory metadata, and stable tag generation.
- Exported the new helpers from `packages/opencode/src/memory/index.ts` and added targeted mapping coverage in `packages/opencode/test/memory/hindsight-mapper.test.ts` for reversible id parsing, metadata fallback, and cross-worktree or indirect-hit rejection.
- Files changed: `packages/opencode/src/memory/hindsight/bank.ts`, `packages/opencode/src/memory/hindsight/mapper.ts`, `packages/opencode/src/memory/index.ts`, `packages/opencode/test/memory/hindsight-mapper.test.ts`, `tasks/prd.json`, `tasks/progress.txt`
- **Learnings for future iterations:**
  - Worktree hash should remain the single shared primitive for both bank ids and document ids so retain, recall, and backfill stay aligned.
  - Resolve logic should prefer reversible `mem:` document ids first, then fall back to `metadata.memory_id`, and treat cross-worktree hashes as hard drops.
  - `sess:` and `obs:` ids should still parse cleanly for diagnostics, but they must remain indirect evidence instead of direct authority candidates.
---

## 2026-04-15 10:48:27 CST - US-002
- Added `packages/opencode/src/memory/hindsight/service.ts` as a workspace-scoped embedded Hindsight lifecycle manager with lazy boot, deterministic loopback base URL and profile metadata, reusable ready state, health checks, degraded fallback, and shutdown on instance disposal.
- Exported the service from `packages/opencode/src/memory/index.ts`, added `@vectorize-io/hindsight-all` and `@vectorize-io/hindsight-client` in `packages/opencode/package.json`, and covered reuse, timeout fallback, and disposal isolation in `packages/opencode/test/memory/hindsight-service.test.ts`.
- Files changed: `packages/opencode/src/memory/hindsight/service.ts`, `packages/opencode/src/memory/index.ts`, `packages/opencode/test/memory/hindsight-service.test.ts`, `packages/opencode/package.json`, `bun.lock`, `tasks/prd.json`, `tasks/progress.txt`
- **Learnings for future iterations:**
  - Persist the embedded `HindsightServer` on state before awaiting startup so timeout or health failures can still shut the daemon down cleanly during degrade.
  - Deriving the embedded port from the shared worktree hash keeps one predictable daemon endpoint per worktree without adding extra config.
  - `tmpdir({ git: true })` is important for worktree-scoped tests because non-git temp directories collapse to the global worktree identity and can mask per-worktree isolation bugs.
---

## 2026-04-15 10:58:55 CST - US-003
- Added `packages/opencode/src/memory/hindsight/client.ts` as the single wrapper around the embedded Hindsight SDK so retain, batch retain, recall, and document or operation inspection all reuse the same loopback service, timeout policy, and non-fatal warning fallback.
- Exported the wrapper from `packages/opencode/src/memory/index.ts` and added `packages/opencode/test/memory/hindsight-client.test.ts` coverage for shared bank routing, inspection helpers, timeout handling, and degraded call failures.
- Files changed: `packages/opencode/src/memory/hindsight/client.ts`, `packages/opencode/src/memory/index.ts`, `packages/opencode/test/memory/hindsight-client.test.ts`, `tasks/prd.json`, `tasks/progress.txt`
- **Learnings for future iterations:**
  - Use one `run()` helper around `MemoryHindsightService.ready()` plus `withTimeout()` so Hindsight callers inherit the same degraded fallback semantics without duplicating guards.
  - Keep the high-level SDK wrapper for retain and recall, but use the generated low-level `sdk` with `createClient(createConfig(...))` for document or operation inspection endpoints the class wrapper does not expose.
  - Client wrapper failures should warn and return `undefined` so local authoritative memory flows stay in control of fallback decisions.
---

## 2026-04-15 11:17:00 CST - US-005
- Added `packages/opencode/src/memory/hindsight/retain.ts` with `MemoryHindsightRetain.memory()` to map one authoritative local memory into one stable `mem:` Hindsight retain call using mapper-derived metadata or tags and replace-style idempotent updates.
- Exported the retain wrapper from `packages/opencode/src/memory/index.ts` and added `packages/opencode/test/memory/hindsight-retain.test.ts` coverage for successful mapping, disabled-mode skip behavior, and non-fatal retain failure results.
- Files changed: `packages/opencode/src/memory/hindsight/retain.ts`, `packages/opencode/src/memory/index.ts`, `packages/opencode/test/memory/hindsight-retain.test.ts`, `tasks/prd.json`, `tasks/progress.txt`
- **Learnings for future iterations:**
  - Keep authoritative-memory retain mapping in one wrapper so create, update, and backfill flows can reuse the same stable `mem:` id and metadata rules.
  - Treat `undefined` retain responses as degraded companion failures and surface them as structured results instead of throwing into local memory write paths.
  - Use targeted Hindsight wrapper tests with `tmpdir({ git: true, config })` plus `spyOn(MemoryHindsightClient, "retain")` when you want to verify mapping without booting the embedded daemon.
---

## 2026-04-15 11:35:00 CST - US-006
- Added `packages/opencode/src/memory/hindsight/recall.ts` as the Hindsight recall query and authority-resolve pipeline that reads ranked hits, resolves only authoritative `mem:` or `metadata.memory_id` references against a local pool, and records unresolved, stale, cross-worktree, and indirect `sess:` evidence as drops.
- Exported the wrapper from `packages/opencode/src/memory/index.ts` and added `packages/opencode/test/memory/hindsight-recall.test.ts` coverage for score or rank propagation, duplicate collapse, indirect-hit dropping, stale detection, and disabled or unavailable fallback behavior.
- Files changed: `packages/opencode/src/memory/hindsight/recall.ts`, `packages/opencode/src/memory/index.ts`, `packages/opencode/test/memory/hindsight-recall.test.ts`, `tasks/prd.json`, `tasks/progress.txt`
- **Learnings for future iterations:**
  - Keep Hindsight recall result parsing tolerant to minor SDK response-shape drift so engine code does not depend on one transient field layout.
  - Preserve rank from raw hit order even when duplicate memory ids collapse, because downstream selection still needs the original evidence ordering.
  - Treat `sess:` and `obs:` hits as indirect ranking evidence with explicit drop reasons instead of letting them masquerade as authoritative local memories.
---
